# Hostinger Compatible Configuration
Options -Indexes

# Protect config files
<FilesMatch "^(db|config|helper)\.php$">
    Order allow,deny
    Deny from all
</FilesMatch>

# Enable URL rewriting if needed
<IfModule mod_rewrite.c>
    RewriteEngine On
</IfModule>
